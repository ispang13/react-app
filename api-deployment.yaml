apiVersion: v1
kind: Service
metadata:
  name: my-restapi
  labels:
    run: my-restapi
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 4000
    protocol: TCP
  selector:
    run: my-restapi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-restapi
spec:
  selector:
    matchLabels:
      run: my-restapi
  replicas: 1
  template:
    metadata:
      labels:
        run: my-restapi
    spec:
      containers:
      - name: testapi
        image: ispang13/test-api:79
        ports:
        - containerPort: 4000